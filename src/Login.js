import React from 'react';
import { Button } from 'react-bootstrap';

function Login() {
    return (
        <div>
            <h1>Login</h1>
            <Button type="primary">
                Hello
          </Button>
        </div>
    );
}

export default Login;