import React from 'react';
import { Button } from 'react-bootstrap';

function Main() {
    return (
        <div>
            <Button type="primary">
                Hello
          </Button>
        </div>
    );
}

export default Main;